import type { ESectionCategory } from '../enums/section-category.enum'

/** String union of all section categories — use ESectionCategory enum for type-safe values */
export type TSectionCategory = ESectionCategory | string
